import React from "react";
const ErrorMessage = props => {
  return (
    <section id="error-message">
      <h1 className="ui huge header">Sommething Went Wrong</h1>
      <p className="ui small header">...and we're not sure what it is!</p>
    </section>
  );
};
export default ErrorMessage;
